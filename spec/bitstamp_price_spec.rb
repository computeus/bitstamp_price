require "spec_helper"

describe BitstampPrice do
  it "has a version number" do
    expect(BitstampPrice::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
